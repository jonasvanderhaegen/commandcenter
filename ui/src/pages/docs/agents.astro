---
import DocsLayout from "../../layouts/DocsLayout.astro";
---

<DocsLayout title="Agents" currentSlug="agents">
  <h1>Agents</h1>
  <p>
    AI coding agents (Claude, Codex, etc.) spawned inside a project, with
    their credentials pulled from the encrypted local credential store
    rather than stored in plaintext on disk. See
    <a href="/docs/credentials">Credential store</a> for how that works.
  </p>
</DocsLayout>
